/rss-proxy/*  https://corsproxy.io/:splat  200

# Let static files serve directly — must come before the SPA catch-all
/sitemap.xml  /sitemap.xml  200
/robots.txt   /robots.txt   200

# SPA fallback — all other routes serve index.html
/* /index.html  200
